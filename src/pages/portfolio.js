import React from 'react'
import { Link } from 'gatsby';
import Layout from '../components/layout'

const portfolio = () => {
    return (
        <div>
            <Layout pageTitle = "Portfolio">
                <h1> These are ma works </h1>
            </Layout>
        </div>
    )
}

export default portfolio;